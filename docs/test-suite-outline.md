# Test Suite Outline

Every `*.test.ts`/`*.test.tsx` file in the repo, with every `describe`/`it` title, transcribed from the source. Covers 44 files and 532 tests as of the last regeneration.

Coverage results for these tests are uploaded to [Codecov](https://app.codecov.io/gh/carpiediem/spoiler-maps) by the [CI workflow](../.github/workflows/ci.yml) on every push to `main` and every pull request. This file itself is regenerated by the [Test Outline workflow](../.github/workflows/test-outline.yml) on every push to `main` — run `npm run docs:test-outline` to update it locally.

## Table of Contents

- [src/App.test.tsx](#srcapptesttsx)
- [src/a11y.test.tsx](#srca11ytesttsx)
- [src/components/EditorSidebar.test.tsx](#srccomponentseditorsidebartesttsx)
- [src/components/MapTimelineControl.test.tsx](#srccomponentsmaptimelinecontroltesttsx)
- [src/components/MapView.test.tsx](#srccomponentsmapviewtesttsx)
- [src/components/StorySelector.test.tsx](#srccomponentsstoryselectortesttsx)
- [src/components/editor-sidebar/BooksSection.test.tsx](#srccomponentseditor-sidebarbookssectiontesttsx)
- [src/components/editor-sidebar/CharactersSection.test.tsx](#srccomponentseditor-sidebarcharacterssectiontesttsx)
- [src/components/editor-sidebar/DescriptionEditorDialog.test.tsx](#srccomponentseditor-sidebardescriptioneditordialogtesttsx)
- [src/components/editor-sidebar/DescriptionPanel.test.tsx](#srccomponentseditor-sidebardescriptionpaneltesttsx)
- [src/components/editor-sidebar/EntryList.test.tsx](#srccomponentseditor-sidebarentrylisttesttsx)
- [src/components/editor-sidebar/MarkersSection.test.tsx](#srccomponentseditor-sidebarmarkerssectiontesttsx)
- [src/components/editor-sidebar/PositionPanel.test.tsx](#srccomponentseditor-sidebarpositionpaneltesttsx)
- [src/components/editor-sidebar/SidebarSection.test.tsx](#srccomponentseditor-sidebarsidebarsectiontesttsx)
- [src/components/editor-sidebar/TelevisionSection.test.tsx](#srccomponentseditor-sidebartelevisionsectiontesttsx)
- [src/components/editor-sidebar/TileUrlHelpDialog.test.tsx](#srccomponentseditor-sidebartileurlhelpdialogtesttsx)
- [src/components/editor-sidebar/books/BookItem.test.tsx](#srccomponentseditor-sidebarbooksbookitemtesttsx)
- [src/components/editor-sidebar/characters/CharacterItem.test.tsx](#srccomponentseditor-sidebarcharacterscharacteritemtesttsx)
- [src/components/editor-sidebar/characters/rangeOptions.test.ts](#srccomponentseditor-sidebarcharactersrangeoptionstestts)
- [src/components/editor-sidebar/television/SeasonItem.test.tsx](#srccomponentseditor-sidebartelevisionseasonitemtesttsx)
- [src/components/view/CharacterPathsPanel.test.tsx](#srccomponentsviewcharacterpathspaneltesttsx)
- [src/components/view/WelcomeDialog.test.tsx](#srccomponentsviewwelcomedialogtesttsx)
- [src/db/client.test.ts](#srcdbclienttestts)
- [src/db/ordering.test.ts](#srcdborderingtestts)
- [src/db/repository.test.ts](#srcdbrepositorytestts)
- [src/db/storage.test.ts](#srcdbstoragetestts)
- [src/lib/attribution.test.ts](#srclibattributiontestts)
- [src/lib/characterInitials.test.ts](#srclibcharacterinitialstestts)
- [src/lib/lastViewedStory.test.ts](#srcliblastviewedstorytestts)
- [src/lib/pinIcon.test.ts](#srclibpinicontestts)
- [src/lib/quadkey.test.ts](#srclibquadkeytestts)
- [src/lib/storyExport.test.ts](#srclibstoryexporttestts)
- [src/lib/storyImport.test.ts](#srclibstoryimporttestts)
- [src/lib/tailConnection.test.ts](#srclibtailconnectiontestts)
- [src/lib/tailFlowClass.test.ts](#srclibtailflowclasstestts)
- [src/lib/tileUrl.test.ts](#srclibtileurltestts)
- [src/lib/timelineHash.test.ts](#srclibtimelinehashtestts)
- [src/lib/timelineVisibility.test.ts](#srclibtimelinevisibilitytestts)
- [src/lib/viewCharacterPins.test.ts](#srclibviewcharacterpinstestts)
- [src/lib/viewTimeline.test.ts](#srclibviewtimelinetestts)
- [src/lib/zoomLimitDetection.test.ts](#srclibzoomlimitdetectiontestts)
- [src/publicSampleStory.test.tsx](#srcpublicsamplestorytesttsx)
- [src/screens/EditScreen.test.tsx](#srcscreenseditscreentesttsx)
- [src/screens/ViewScreen.test.tsx](#srcscreensviewscreentesttsx)

## src/App.test.tsx

- **App routes**
  - redirects / to /edit
  - redirects /?d=<url> to /view?d=<url> instead of /edit
  - renders the editor at /edit
  - renders the view screen at /view, with no map specified
  - has exactly one main landmark once / redirects to /edit
  - has exactly one main landmark at /view with no map specified (error state)

## src/a11y.test.tsx

- **accessibility: all content is contained by a landmark**
  - has no orphaned content on a fresh "New Map"
  - has no orphaned content with a saved story and an expanded book accordion
  - has no orphaned content on the view screen once loaded

## src/components/EditorSidebar.test.tsx

- **EditorSidebar**
  - disables Save until the form has unsaved changes, then disables it again after saving
  - starts with Save disabled for a brand new map
  - calls onSave with a valid name, tile URL template, and default initial position
  - includes the chosen palette when saving, and shows its swatches once selected
  - extrapolates a {q} template from a real example tile URL
  - shows an error and does not call onSave when the name is blank
  - shows an error and does not call onSave for an invalid tile URL
  - prefills the form from the selected story
  - falls back to "Default" for a palette key that no longer matches a known palette
  - saves the tile layer author and attribution URL when both are provided
  - saves an updated zoom range
  - rejects a zoom range whose maximum is below its minimum
  - auto-detects the max zoom by probing the tile URL once the field settles
  - leaves the zoom range alone when auto-detection is inconclusive
  - debounces detection, only probing the URL the user settled on
  - cancels an in-flight probe on unmount, without warning about a state update after unmount
  - clears the form when switching to "New Map"
  - forwards story selection from the story selector
  - disables the Export menu item until a story is selected, and calls onExportStory when clicked
  - shows a Delete Story button only once a story is selected
  - asks for confirmation before deleting, and only calls onDeleteStory once confirmed
  - captures the current map position and reflects it in the display and on save
  - leaves the displayed position unchanged when the map is not ready
  - hides the tile layer author/attribution fields until the tile URL template is valid
  - opens and closes the tile URL template help dialog
  - shows one section at a time, collapsing the previous one when another is opened
  - renders the Television, Characters, and Markers sections
  - hides the Books/Television/Characters/Markers sections for a brand new, unsaved map
  - re-collapses to the Map section when switching stories
  - opens the Books section from a #books URL hash
  - expands the Nth book from a #books-N URL hash
  - expands the Nth character from a #characters-N URL hash
  - ignores a #books hash for a brand new, unsaved map
  - ignores a hash that does not name a known section
  - responds to the hash changing while already open
  - lists a newly saved position once the user returns from editing it
  - reopens an existing position, prefilled, when it is clicked in the list

## src/components/MapTimelineControl.test.tsx

- **MapTimelineControl**
  - renders nothing when there are no books or seasons
  - shows a placeholder and disables navigation when the story has a book but no chapters
  - clicking the already-active mode button leaves the mode unchanged
  - defaults to book mode, starts on the last chapter, and reports it
  - defaults to tv mode when the story has only seasons
  - starts on initialMode/initialIndex instead of the default, e.g. from a #chapter-N URL fragment
  - starts on initialMode "tv", e.g. from a #episode-N URL fragment
  - clamps an out-of-range initialIndex to the active medium’s actual length
  - applies initialIndex without an initialMode, leaving the mode at its default
  - switches modes, jumping to the new medium’s last entry
  - steps backward and forward with the arrow buttons, disabling them at the ends
  - moves the scrub position via the arrow keys while the slider itself is focused
  - moves the scrub position via the slider’s own value change (e.g. a drag), not just arrow keys
  - hyperlinks the label when the current chapter has a URL
  - does not hyperlink the label when the current chapter has no URL
  - shows an optional heading above the panel
  - steps via the arrow keys when a non-slider control within the panel has focus
  - does not double-step when the slider itself has focus
  - keeps stepping at a steady rate while an arrow key is held, stopping on key up
  - clears the previous interval when a new arrow key is pressed before the old one is released
  - ignores keyUp for an unrelated key, leaving a held arrow key’s interval running
  - ignores browser-repeated keydown events, relying on its own interval instead

## src/components/MapView.test.tsx

- **MapView**
  - renders the default xyz tile layer when no tileUrl is set
  - renders an xyz tile layer for a custom {x}/{y}/{z} template
  - renders the quadkey tile layer for a {q} template
  - renders at the given center and zoom, and exposes the map instance via mapRef
  - calls onPositionChange when the map is panned or zoomed
  - does not render a draft position marker when draftPosition is not set
  - renders a draggable draft position marker and reports its new lat/lng on drag end
  - does not render character position pins when none are given
  - renders a non-draggable, numbered pin for each character position
  - renders a skull marker, not a numbered pin, for a dead position
  - calls onCharacterPositionPinClick when a static pin is clicked
  - binds a tooltip showing the position’s note
  - binds no tooltip — never the raw lat/lng — when the position has no note
  - renders a solid colored dot, not a labeled pin, for a "dot"-style position
  - binds a tooltip showing the note on a "dot"-style position too
  - renders a "dot"-style position under active editing as the usual draggable pin instead
  - renders the pin matching editingPositionId as draggable, tracking draftPosition
  - does not render the generic draft marker while an existing pin is being edited
  - calls onTailPointClick with the clicked lat/lng while drawing a tail
  - does not listen for map clicks when not drawing a tail
  - renders the in-progress tail as a colored polyline with a dot per point
  - does not render a tail when there is no draft position yet
  - renders a polyline for each character tail overlay
  - renders a multi-point tail as a single polyline tagged for the flow animation
  - applies the initial zoom limits to the underlying Leaflet map
  - keeps the live map in sync when zoom limits change without remounting

## src/components/StorySelector.test.tsx

- **StorySelector**
  - shows "New Map" when no story is selected
  - shows the selected story name as the title
  - links the title to the view screen for the selected story
  - does not link the title when no story is selected (New Map)
  - disables the Export option until a story is selected, and calls onExportStory when clicked
  - opens the menu and lists every story plus a New Map option
  - calls onSelect with a story id when a story is chosen
  - calls onSelect with null when "New Map" is chosen
  - filters the list by the search query
  - shows an empty state when the search query matches nothing
  - closes the menu when clicking outside
  - closes the menu when pressing Escape
  - toggles the menu closed when the trigger is clicked again
  - clicks the hidden file input when "Import from file…" is clicked
  - imports the selected file and closes the menu on success
  - shows an error message and keeps the menu open when import fails
  - stringifies a non-Error rejection from onImportFile
  - does nothing when the file picker is opened but no file is chosen
  - clears a previous import error when the menu is reopened

## src/components/editor-sidebar/BooksSection.test.tsx

- **BooksSection**
  - shows a loading state, then "No books yet." for a story with none
  - lists existing books with their chapter counts
  - adds a new book at the bottom of the list, expanded, and persists it
  - auto-expands the book at the 1-based initialExpandedIndex
  - reflects book edits and added chapters, and collapses when the accordion is closed again
  - editing one book does not affect a sibling book
  - deletes a book from the database and the list, collapsing back to nothing expanded
  - does not update state after unmounting while books are still loading
  - ignores an out-of-range initialExpandedIndex

## src/components/editor-sidebar/CharactersSection.test.tsx

- **CharactersSection**
  - shows a loading state, then "No characters yet." for a story with none
  - lists existing characters
  - reports the character count for the story once loaded
  - reports zero for a story with no characters
  - adds a new character, expanded, and persists it
  - auto-expands the character at the 1-based initialExpandedIndex
  - ignores an out-of-range initialExpandedIndex
  - collapses a character when its accordion is closed again
  - editing one character does not affect a sibling character
  - deletes a character from the database and the list, collapsing back to nothing expanded
  - reorders characters via drag and drop, persisting the new sort order
  - un-dims the dragged row once the drag ends without a drop (e.g. cancelled)
  - reorders to the very start of the list when dropped on the first character
  - does nothing when a character is dropped on itself
  - does nothing when a drop lands without a drag having started
  - does not update state after unmounting while characters are still loading
  - reports numbered pins for the expanded character once its positions have loaded
  - fades the expanded character’s tails from 20% opacity (oldest) to full (most recent)
  - connects a tail to the nearest preceding visible position for the expanded character, skipping a hidden one in between
  - draws a straight tail to the preceding position even when a position has no tail of its own
  - clears the pins once the expanded character is collapsed again
  - collapses the expanded character when the Characters section itself collapses
  - reports the newly expanded character’s pins when switching directly between two expanded characters
  - shows an initialed pin for the last position, a dot for earlier ones, plus every tail, once toggled visible while collapsed
  - skips the visible-toggle pin for the currently expanded character, since it is already shown numbered
  - clears a deleted character out of the visible set, without leaving it toggled on for a later character
  - calls onEditPosition with (characterId, index, position) when a list item is clicked
  - hides an expanded character’s position once the timeline is before its start chapter
  - skips a visible-but-collapsed character’s not-yet-reached positions, marking the last reached one as the pin
  - connects a visible-but-collapsed character’s tail to the nearest preceding visible position, skipping a hidden one

## src/components/editor-sidebar/DescriptionEditorDialog.test.tsx

- **DescriptionEditorDialog**
  - renders the existing description and saves the edited markdown
  - runs each toolbar button (Bold, Italic, Bulleted list, Numbered list, Quote) without error
  - closes without saving when Cancel is clicked
  - closes via the close icon

## src/components/editor-sidebar/DescriptionPanel.test.tsx

- **DescriptionPanel**
  - shows a placeholder when there is no description yet
  - shows the description text when set
  - opens the rich-text editor dialog when the edit button is clicked

## src/components/editor-sidebar/EntryList.test.tsx

- **EntryList**
  - renders no rows or column headers when there are no items
  - creates an item via onCreate and reports it to the parent
  - appends a new item after existing ones
  - renames an item on blur and persists it via onUpdate
  - edits an item URL on blur and persists it, storing a blank value as null
  - renaming one item does not affect a sibling item
  - moves focus between name fields with the arrow keys
  - moves focus between URL fields with the arrow keys, independently of the name column
  - does nothing when the arrow keys would move focus past the first or last item
  - leaves other keys alone
  - deletes an item via onDelete and removes it from the list

## src/components/editor-sidebar/MarkersSection.test.tsx

- **MarkersSection**
  - renders the placeholder text
  - reports the total marker count across all marker sets for the story
  - reports zero for a story with no marker sets
  - does not update state after unmounting while marker sets are still loading

## src/components/editor-sidebar/PositionPanel.test.tsx

- **PositionPanel**
  - shows the position index in the header and calls onBack
  - prompts to drag the pin when no position is set yet
  - shows the lat/lng once a position is set
  - hides the Chapter Range and Episode Range sections when there are no books or seasons
  - shows the Chapter Range section with an overall-indexed, book-grouped chapter list
  - shows the Episode Range section with an overall-indexed, season-grouped episode list
  - falls back to "Untitled Book"/"Untitled Chapter" for blank names
  - falls back to "Untitled Episode" for a blank episode name
  - selects a chapter as the range start, and can be set back to open
  - toggles the Dead checkbox
  - types into the Note field
  - shows the "Add a tail" button with its tooltip, disabled until a position is set
  - shows Save/Cancel instead of the tail button while drawing, and Save persists the drawn points
  - discards the drawn points when Cancel is clicked
  - does not update state after unmounting while books/seasons are still loading
  - does not persist a position while it stays at its initial value, even if fields change
  - creates the position once the marker moves from its initial value
  - saves a typed note, trimmed, as part of the created position
  - clears a blank note back to null
  - updates the same position row on further marker moves and field changes
  - prefills its fields from an existing position
  - prefills the chapter and episode range selects from an existing position
  - does not write to the database merely from opening an existing position
  - updates (rather than duplicates) an existing position when its marker moves

## src/components/editor-sidebar/SidebarSection.test.tsx

- **SidebarSection**
  - shows no count chip when count is omitted
  - hides the count chip when count is zero
  - shows a count chip when count is greater than zero
  - sticks the header button to the top of its scroll container, with an opaque background so scrolled content can’t show through

## src/components/editor-sidebar/TelevisionSection.test.tsx

- **TelevisionSection**
  - shows a loading state, then "No television seasons yet." for a story with none
  - lists existing seasons by position, with their episode counts
  - adds a new season at the bottom of the list, expanded, and persists it
  - auto-expands the season at the 1-based initialExpandedIndex
  - reflects season edits and added episodes, and collapses when the accordion is closed again
  - editing one season does not affect a sibling season
  - deletes a season from the database, shifting the remaining season label
  - does not update state after unmounting while seasons are still loading
  - ignores an out-of-range initialExpandedIndex

## src/components/editor-sidebar/TileUrlHelpDialog.test.tsx

- **TileUrlHelpDialog**
  - renders nothing when closed
  - shows the placeholder scheme explanation when open
  - calls onClose when the close button is clicked
  - calls onClose when pressing Escape
  - unmounts the dialog once onClose sets open to false

## src/components/editor-sidebar/books/BookItem.test.tsx

- **BookItem**
  - shows the book name and chapter count in the summary, with a tooltip spelling out the count
  - falls back to "Untitled Book" and singular "chapter" in the tooltip
  - edits and persists the name, author, and Wiki URL fields on blur
  - stores blank author/url as null
  - does not touch the story when persisting a book edit
  - adds a chapter and renames it via the chapter editor dialog, persisting both
  - asks for confirmation before deleting, and does not delete when cancelled
  - falls back to "Untitled Book" in the delete confirmation title
  - closes the delete confirmation on Escape without deleting
  - calls onDelete once the deletion is confirmed

## src/components/editor-sidebar/characters/CharacterItem.test.tsx

- **CharacterItem**
  - shows the character name in the summary
  - sticks below the SidebarSection header, with an opaque background on the button itself
  - dims the row while it is the one being dragged
  - falls back to "Unnamed Character" in the summary
  - edits and persists the name, group, icon URL, and URL fields on blur
  - stores blank group/icon/url as null
  - edits and persists the color field on blur
  - asks for confirmation before deleting, and does not delete when cancelled
  - falls back to "Unnamed Character" in the delete confirmation title
  - calls onDelete once the deletion is confirmed
  - starts hidden, and toggles the visibility icon/label without collapsing the accordion
  - calls onAddPosition with 1 for a character with no existing positions
  - calls onAddPosition with the next 1-based index for a character with existing positions
  - lists existing positions, labeled with their chapter/episode range
  - shows a terse chapter/episode range summary, with the full titles in a tooltip
  - outlines a position’s avatar instead of filling it once the timeline is before its start chapter
  - shows a position’s note instead of its lat/lng when one is set
  - calls onEditPosition with the position and its 1-based index when a list item is clicked
  - reports its loaded positions via onPositionsChange, regardless of expanded state
  - does not render a positions list for a character with none
  - shows the character icon in the summary instead of the color swatch when set
  - falls back to "Unnamed Character" as the icon alt text for a blank name

## src/components/editor-sidebar/characters/rangeOptions.test.ts

- **toAcronym**
  - takes the first letter of each word, uppercased
  - collapses repeated whitespace
- **flattenEpisodeOptions**
  - labels each episode with a zero-padded SxxExx code and an overall running index
  - pads season/episode numbers past 9 into double digits, and falls back for an untitled episode
- **summarizePositionRange**
  - hides both when the ranges are null
  - hides a range whose boundaries are both explicitly unset
  - summarizes a chapter range with both boundaries set
  - collapses to a single value when both boundaries name the same chapter
  - describes a chapter range open on the start
  - describes a chapter range open on the end
  - summarizes an episode range
  - summarizes both a chapter range and an episode range together
  - falls back to "#id" when a boundary chapter/episode is not in the options list

## src/components/editor-sidebar/television/SeasonItem.test.tsx

- **SeasonItem**
  - shows the season position and episode count in the summary, with a tooltip spelling out the count
  - uses singular "episode" in the tooltip for a single episode
  - edits and persists the URL field on blur
  - stores a blank URL as null
  - adds an episode and renames it via the episode editor dialog, persisting both
  - asks for confirmation before deleting, and does not delete when cancelled
  - calls onDelete once the deletion is confirmed

## src/components/view/CharacterPathsPanel.test.tsx

- **CharacterPathsPanel**
  - shows an empty state when the story has no characters
  - sticks the header to the top of the panel, with an opaque background so the list can’t show through
  - shows an icon image when set, "Unnamed Character" when the name is blank, and a default swatch color when unset
  - links the name to the character’s own URL when set, plain text otherwise
  - lists every character, unchecked by default
  - toggles one character on click
  - unchecks an already-checked character
  - checks every character via the select-all control, and shows indeterminate for a partial selection
  - unchecks everything via the select-all control once all are checked
  - toggles the full-path setting, with a tooltip reflecting the current state
  - labels the toggle "Current locations only" once full paths are already shown

## src/components/view/WelcomeDialog.test.tsx

- **WelcomeDialog**
  - is hidden when not open
  - shows the explainer text when open
  - calls onClose when dismissed

## src/db/client.test.ts

- **getDatabase**
  - creates a fresh database with the schema applied
  - reuses the same connection on subsequent calls
  - enforces foreign key constraints
- **persist**
  - reloads previously persisted data after resetting the connection
- **migrations**
  - upgrades a database stored under an older schema version, preserving its data
  - records the schema version in PRAGMA user\_version after creating a database
  - survives a migration whose column was already applied without being recorded
  - rethrows a migration failure unrelated to an already-applied column

## src/db/ordering.test.ts

- **sortOrderAfter**
  - starts an empty list at 0
  - appends after the largest existing sort order
- **sortOrderBetween**
  - starts an empty list at 0
  - inserts before the first item
  - inserts after the last item
  - inserts at the midpoint between two items

## src/db/repository.test.ts

- **stories**
  - creates and lists stories
  - gets a story by id, or null if it does not exist
  - updates a story
  - deletes a story, cascading to its children
- **books and chapters**
  - creates, lists, updates, and deletes books for a story
  - creates, lists, updates, and deletes chapters for a book, cascading on book delete
- **tv seasons and episodes**
  - creates, lists, updates, and deletes tv seasons for a story
  - creates, lists, updates, and deletes episodes for a season, cascading on season delete
- **marker sets and markers**
  - creates, lists, updates, and deletes marker sets for a story
  - creates, lists, updates, and deletes markers for a marker set, cascading on marker set delete
  - round-trips a polygon
- **characters**
  - creates, lists, updates, and deletes characters for a story
- **character positions**
  - creates, lists, updates, and deletes a position with no ranges
  - round-trips a tail
  - creates a position already marked dead
  - round-trips a bounded chapter range
  - allows a single-chapter range (start equal to end)
  - allows a chapter range spanning two different books
  - rejects a chapter range whose end comes before its start
  - rejects a chapter range referencing a chapter that does not exist
  - round-trips an open-ended chapter range
  - normalizes a chapter range with both boundaries open to no range at all
  - round-trips a bounded episode range
  - round-trips an open-ended episode range
  - rejects an episode range whose end comes before its start
  - rejects an episode range referencing an episode that does not exist
  - clears a range boundary when its chapter is deleted, without deleting the position
  - deletes a position when its character is deleted

## src/db/storage.test.ts

- **storage**
  - returns null when nothing has been saved yet
  - round-trips saved bytes, with no schema version recorded alongside them
  - overwrites a previously saved database
  - round-trips a legacy record’s sibling schema version, for backward compatibility
  - overwrites a legacy record entirely when a later save omits the schema version
  - rejects when opening the underlying database fails

## src/lib/attribution.test.ts

- **buildTileAttribution**
  - combines an author and attribution URL into an HTML link
  - trims surrounding whitespace from both fields
  - escapes HTML-significant characters in both fields
  - returns null when the author is missing
  - returns null when the attribution URL is missing
  - returns null when both are missing

## src/lib/characterInitials.test.ts

- **characterInitials**
  - combines the first letter of the first and last words
  - uses the first and last of more than two words
  - takes the first two letters of a single-word name
  - uppercases the result
  - collapses extra whitespace
  - falls back to "?" for an empty name
  - ignores parenthesized text

## src/lib/lastViewedStory.test.ts

- **lastViewedStory**
  - returns null when nothing has been remembered
  - remembers and returns a set story id
  - returns null for a corrupted (non-numeric) stored value

## src/lib/pinIcon.test.ts

- **buildPinIcon**
  - builds a pin sized for a single-character label
  - shrinks the font for a two-character label
  - accepts a color with or without a leading #
  - uses white label text on a dark fill color
  - uses black label text on a light fill color
  - sizes and anchors the icon for a teardrop pin
- **buildSkullIcon**
  - draws a skull from shapes, not a text glyph
  - fills the skull white with black lines, regardless of character color
  - is the same width as a numbered pin, sized and anchored at its own center

## src/lib/quadkey.test.ts

- **toKeyholeQuadkey**
  - returns just the prefix at zoom 0
  - encodes each quadrant at zoom 1
  - encodes nested quadrants at zoom 2
- **extrapolateQuadkeyTemplate**
  - replaces a quadkey filename with a {q} placeholder
  - replaces a quadkey that is its own path segment
  - returns null when no segment looks like a quadkey
  - returns null when more than one segment looks like a quadkey

## src/lib/storyExport.test.ts

- **buildStoryDocument**
  - throws for a story that does not exist
  - builds the top-level story fields, omitting unset optional ones
  - includes tile fields when set
  - includes description and paletteKey when set, omits them when unset
  - nests chapters under their book, omitting unset author/url
  - includes a book author/url when set
  - nests episodes under their season
  - includes an episode url when set
  - resolves a chapter range spanning two books to flat 0-based indices
  - represents an open-ended range with a null boundary
  - represents a range with an open start as a null first boundary
  - includes a dead/note/tail position, omitting them for a default one
  - resolves an episode range and a season url/character icon
  - represents an episode range with an open start as a null first boundary
  - nests markers under their marker set, with a polygon and range when set
  - omits an unset marker color, and includes an episode range when set
- **exportStoryToYaml**
  - serializes the document to a YAML string

## src/lib/storyImport.test.ts

- **parseStoryDocument**
  - parses a minimal valid document
  - defaults formatVersion to 1 when the field is absent, for files exported before it existed
  - accepts an explicit formatVersion no newer than this app understands
  - rejects a formatVersion newer than this app understands
  - throws a clear error for invalid YAML syntax
  - throws when the document is not a mapping
  - throws when a required field is missing
  - throws when a range tuple is malformed
  - throws when an optional string field has the wrong type
  - parses television seasons/episodes, marker sets/markers, and a tail
  - defaults each nested list to empty when its key is omitted entirely
  - defaults a marker set with no markers key to an empty list
- **importStoryDocument**
  - creates a new story with " v2" appended to its name
  - creates books and chapters in order
  - creates tv seasons and episodes in order
  - resolves a chapter range tuple spanning two books back to chapter ids
  - resolves an open-ended range (null boundary)
  - creates marker sets and markers, resolving their ranges
  - deletes the partially created story and rethrows when a range references an out-of-bounds index
- **importStoryFromYaml**
  - parses and imports in one step
- **round trip**
  - exports and re-imports a story with the same structure (aside from the name suffix)

## src/lib/tailConnection.test.ts

- **buildTailPoints**
  - starts with the position itself, then its own tail waypoints
  - appends the preceding position’s own lat/lng, when there is one
  - treats a missing or null tail as empty
  - draws a straight line to a preceding position even with no tail of its own
- **hasTailToDraw**
  - is false with no tail and no preceding position
  - is true when the position has its own tail waypoints
  - is true when there is a preceding position, even with no tail of its own
- **applyTailOpacityGradient**
  - produces nothing for an empty list
  - draws a single tail at full opacity
  - fades linearly from 20% opacity for the first (oldest) tail to full for the last (most recent)
  - preserves the other fields on each tail

## src/lib/tailFlowClass.test.ts

- **attachTailFlowClass**
  - does nothing for a null instance
  - adds the class immediately when the element already exists
  - does nothing yet if the element doesn’t exist when first called
  - registers a persistent "add" listener (not once), re-applying the class every time Leaflet fires it

## src/lib/tileUrl.test.ts

- **isValidTileUrlTemplate**
  - accepts a well-formed tile URL template
  - accepts a well-formed quadkey tile URL template
  - accepts http URLs
  - rejects an empty string
  - rejects a URL missing placeholders
  - rejects a URL missing the {y} placeholder
  - rejects a malformed URL even with placeholders present
  - rejects a URL without a protocol
- **detectTileUrlTemplateKind**
  - detects an xyz template
  - detects a quadkey template
  - prefers quadkey when both placeholder styles are present
  - returns null when no recognized placeholders are present
- **resolveTileUrlTemplate**
  - resolves an explicit xyz template as-is
  - resolves an explicit quadkey template as-is
  - extrapolates a quadkey template from a real example tile URL
  - returns null for a plain URL with no template and no extrapolatable quadkey
  - returns null for an empty string

## src/lib/timelineHash.test.ts

- **parseTimelineHash**
  - parses a #chapter-N fragment into book mode
  - parses a #episode-N fragment into tv mode
  - is case-insensitive
  - returns null for an empty hash
  - returns null for an unrelated hash
  - returns null for a fragment missing its index
  - returns null for a zero index

## src/lib/timelineVisibility.test.ts

- **makeTimelineVisibilityChecker**
  - is visible when its start chapter is at the timeline index
  - is visible when its start chapter is before the timeline index
  - is hidden when its start chapter is after the timeline index
  - is visible when the chapterRange is entirely unset
  - is visible when the start chapter is open (null)
  - is visible when the start chapter id is not in the options list
  - is hidden in book mode when the position is restricted only by an episode range
  - is hidden in tv mode when the position is restricted only by a chapter range
  - is visible in both modes when neither range is set
  - checks the episode range instead of the chapter range in tv mode

## src/lib/viewCharacterPins.test.ts

- **buildViewPinsAndTails**
  - produces nothing for an unchecked character
  - shows the last reached position as a pin and earlier ones as dots
  - hides positions the timeline scrub has not reached yet
  - omits tails when showFullPath is false, includes them when true
  - draws a straight tail to the preceding position even when a position has no tail of its own
  - produces nothing for a checked character whose positions have not been reached yet
  - connects a tail to the nearest preceding visible position, skipping a hidden one in between
  - in book mode, skips a position gated only by an episode range when finding the preceding position for a tail
  - only includes checked characters, keyed by their array index

## src/lib/viewTimeline.test.ts

- **buildDocumentChapterOptions**
  - flattens every book’s chapters in order, with a growing 0-based index
  - falls back to "Untitled Book"/"Untitled Chapter" for blank names
- **buildDocumentEpisodeOptions**
  - falls back to "Untitled Episode" for a blank name
  - flattens every season’s episodes in order, with an SxxExx code
- **isPositionVisible**
  - is always visible when the range for the active medium is unset
  - is visible once the 1-based current index passes the 0-based start boundary
  - checks the episode range only in tv mode
  - is hidden in book mode when the position is restricted only by an episode range
  - is hidden in tv mode when the position is restricted only by a chapter range
  - is visible in both mediums when neither range is set at all

## src/lib/zoomLimitDetection.test.ts

- **latLngToTile**
  - maps the world origin to the single tile at zoom 0
  - maps the prime meridian/equator to the center tiles at a higher zoom
  - maps the northwest corner to tile (0, 0)
  - clamps to the last tile instead of overflowing past the southeast corner
- **buildProbeUrl**
  - substitutes x/y/z into an xyz template
  - substitutes a keyhole quadkey into a quadkey template
- **detectMaxZoom**
  - returns null when even the minimum zoom has no tile
  - returns the probe ceiling when the tile set goes at least that far
  - binary-searches for the boundary between present and missing tiles
  - respects a non-zero minZoom as the search floor
  - returns null when a probe throws (e.g. a CORS-blocked fetch)
  - returns null when the probe ceiling check throws
  - returns null when a probe during the binary search throws
  - uses fetch with a HEAD request by default

## src/publicSampleStory.test.tsx

- **public/sample-story.yaml**
  - renders end-to-end on the view screen, dialog and all

## src/screens/EditScreen.test.tsx

- **App**
  - starts on a fresh "New Map" and saves it as a new story
  - has exactly one level-one heading, naming the selected story once one is saved
  - never skips a heading level, even with a book accordion expanded
  - loads existing stories, switches between them, and updates the selected one
  - populates the form when /edit/<id> is loaded directly, not just via in-app navigation
  - saves an edited description and reflects it in the sidebar preview
  - redirects a bare /edit to the remembered last-viewed story, when it still exists
  - falls back to the create-new flow for a URL naming a non-numeric story id
  - shows the pushpin only once the map has moved, and captures its live position
  - shows a draggable map marker while editing a character position, and hides it again on back
  - opens the Position panel, prefilled, when a map pin is clicked
  - does not move the map when the position being edited is already in view
  - draws a tail by clicking the map, then saves it onto the position
  - does not update state after unmounting while stories are still loading
  - shows the map timeline control for a story with books, and reports the initial scrub position
  - starts the timeline slider from a #chapter-N URL fragment instead of the last chapter
  - imports a YAML file as a brand-new story, selecting it once done
  - auto-imports a story from /edit?d=<url> and starts editing it
  - shows a dismissible error, without crashing, when the /edit?d=<url> fetch fails
  - shows a stringified error when the /edit?d=<url> fetch rejects with a non-Error value
  - does not update state after unmounting while a /edit?d=<url> import is still in flight
  - does not update state after unmounting while a failing /edit?d=<url> import is still in flight
  - ignores a ?d= param when a specific story id is already in the URL
  - exports the selected story, downloading it as a YAML file
  - falls back to "story.yaml" when the story name has no sluggable characters
  - deletes the selected story once confirmed, returning to a fresh "New Map"
  - contains all content within landmark regions

## src/screens/ViewScreen.test.tsx

- **ViewScreen**
  - shows an error when neither a story id nor a data URL is given
  - shows an error when the data URL fetch fails
  - shows an error when the fetched document fails validation
  - renders the map, timeline heading, and character list from a fetched data URL
  - has a level-one heading in the error state (no story id or data URL given)
  - has a level-one heading in the loading state
  - has exactly one level-one heading, named after the story, once loaded
  - has a main landmark in the error state (no story id or data URL given)
  - has a main landmark in the loading state
  - has exactly one main landmark once loaded
  - starts the timeline slider from a #chapter-N URL fragment instead of the last chapter
  - renders a local story directly from the database, given a story id and no data URL
  - shows a DescriptionDialog with the story name and description instead of the WelcomeDialog, when set
  - renders the description as HTML, not literal Markdown syntax
  - shows a pin once a character is checked, respecting the spoiler slider
  - shows full paths by default, hiding tails once toggled to "Current locations only"
  - shows the welcome dialog on open, and closes it on dismiss
  - shows the welcome dialog again on a fresh visit, even after a prior dismissal
  - reloads when navigating from one data URL to another without unmounting
  - does not update state after unmounting while the document is still loading
  - does not update state after unmounting while the document load is failing
  - stringifies a non-Error rejection from a failed fetch
